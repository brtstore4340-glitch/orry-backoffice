export default function Loading() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", background: "#040914", color: "#e8f5ff" }}>
      <div style={{ opacity: 0.9, fontFamily: "Ubuntu, Noto Sans Thai, sans-serif", letterSpacing: "0.08em" }}>
        Loading Forge Omega...
      </div>
    </main>
  );
}