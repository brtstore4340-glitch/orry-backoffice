# Code Comment Rules

- Add comments only when the code would otherwise hide a non-obvious business rule, safety constraint, or framework nuance.
- Prefer comments that explain why, not comments that repeat what the code already says.
- For ORRY, comments are most justified around approval gates, stock timing, auth boundaries, and tricky Next.js server/client constraints.
